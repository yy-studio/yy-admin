<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yystudio.admin.module.business.law.dao.ReadingRecordDao">
    <update id="updateReadDuration">
        update t_reading_record set read_duration = #{readDuration}
        where id = #{id}
    </update>
    <update id="updateProgress">
        update t_reading_record set progress = #{progress}
        where id = #{id}
    </update>

    <select id="queryPage" resultType="com.yystudio.admin.module.business.law.domain.vo.ReadingRecordVO">
        SELECT
        r.*,u.username,c.title
        FROM t_reading_record r
        left join t_user u on r.user_id = u.id
        left join t_content c on r.content_id = c.id
        <where>
            <!--用户ID-->
            <if test="queryForm.userId != null">
                AND r.userId = #{queryForm.userId}
            </if>
            <!--文章ID-->
            <if test="queryForm.contentId != null">
                AND r.contentId = #{queryForm.contentId}
            </if>
            <!-- 阅读类型 -->
            <if test="queryForm.readType != null">
                AND r.read_type = #{queryForm.readType}
            </if>
            order by r.create_time desc
        </where>
    </select>
</mapper>