<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yystudio.admin.module.business.law.dao.ContentCollectDao">

    <select id="queryPage" resultType="com.yystudio.admin.module.business.law.domain.vo.ContentCollectVO">
        SELECT
        r.*,u.username,c.title, c.subtitle, c.type, c.category_id, c.cover_image, c.tags
        FROM t_content_collect r
        left join t_user u on r.user_id = u.id
        left join t_content c on r.content_id = c.id
        <where>
            <!--用户ID-->
            <if test="queryForm.userId != null">
                AND r.user_id = #{queryForm.userId}
            </if>
            <!--文章ID-->
            <if test="queryForm.contentId != null">
                AND r.content_id = #{queryForm.contentId}
            </if>
            order by r.create_time desc
        </where>
    </select>
</mapper>